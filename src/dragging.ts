// @ts-nocheck
export { dragging }
const display = document.querySelector(".display")!;

const dragging = (e) => {
  /* const x = e.clientX;
  const y = e.clientY;
  display.addEventListener("mousemove", () => {
    console.log(x)
    display.style.left = x;
  }) */
  
  
}